// converting infix expression to postfix recursive
#include <iostream>
#include <stack>
#include <string>
using namespace std;

// recursive function - takes in a char -> decide what to do 
char ITP(string infix, int len, int i){
    // base case - stops the recursion when i is at the last character of string 
    if (i == len -1){
        //break?

    }
    // general case - reduced to base case 
    ITP(c);
}

int main(){

}